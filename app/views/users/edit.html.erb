<% javascript 'new-user' %>
<%= render :partial => "users/user_nav"  %>
<%= title "Edit your details", :h2  %>
<% form_for @current_user do |f| %>
  <%= render :partial => "form", :locals => {:f => f, :with_password => false }   %>
  <div class="buttons">
    <label for=""></label>
    <%= submit_tag 'Update' %>
  </div>
<% end %>