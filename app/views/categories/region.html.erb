<%= title "#{@category.name} in #{@region.name}" %>
    <ul class="categories simple">
      <% @users.each do |u| -%>
        <li class="<%=cycle('odd', 'even')%>"><%= render :partial => "users/user", :locals => {:user => u} %> </li>
      <% end -%>
    </ul>
